# MIDI_Macro
An app to map macros to a MIDI device

## Install dependencies

1. (Optional) Create and activate a virtual environment.
2. Install required packages:

```powershell
pip.exe install -r dependencies.txt
```

## Run

```powershell
python.exe MIDI_Macro.py
```
